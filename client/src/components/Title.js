import { Typography } from '@mui/material';
import React from 'react';

const Title = () => {
  return (
    <Typography variant="h3" sx={{ color: 'aliceblue', margin: '2rem 0' }}>
      Nagwa Test
    </Typography>
  );
};

export default Title;
